#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../z-schema/bin/z-schema" "$@"
else
  exec node  "$basedir/../../../../z-schema/bin/z-schema" "$@"
fi
# cmd-shim-target=C:/Users/Carlos Cortez/OneDrive/Documents/sistema_parroquial/auth-node/node_modules/.pnpm/@apidevtools+swagger-parser@10.0.3_openapi-types@12.1.3/node_modules/z-schema/bin/z-schema
